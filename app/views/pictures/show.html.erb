<%= link_to("Back to Pictures", "/pictures") %> | <%= link_to("Edit", "/pictures/#{@picture.id}/edit") %>
|

<!-- data: {confirm: "..." } makes a confirmation popup appear when the user clicks this link. method: lets Rails know what HTTP request to use on the URL. So the URL is /picture/#{@picture.id}, and the HTTP request type is DELETE (as opposed to the usual GET request generated by a link). -->

<%= link_to("Delete", "/pictures/#{@picture.id}", method :delete, data: {confirm: "Are you sure you want to delete this picture?"}) %>


<p><%= @picture.title%> by  <%=@picture.artist%></p>

<%= image_tag(@picture.url)%>
